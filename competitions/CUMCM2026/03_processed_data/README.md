# 03_processed_data

用于保存由代码生成的处理后数据。

## 子目录

- `interim/`：中间数据，可重复生成，默认不纳入 Git。
- `final/`：清洗完成、可用于建模和论文的处理后数据。
- `logs/`：数据处理日志和字段变更说明。

## 规则

- 本目录中的数据必须由 `04_code/` 中的脚本生成。
- 不允许手工改写处理结果后直接用于论文。
- 每次关键清洗规则变化都应记录在日志或代码注释中。
- 输出文件命名应包含处理阶段和含义，不使用 `new`、`final2`、`latest` 等模糊名称。
