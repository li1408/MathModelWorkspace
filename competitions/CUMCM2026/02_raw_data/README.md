# 02_raw_data

用于保存官方原始数据和附件。原始数据不得被程序覆盖或手工改写。

## 必做记录

- `data_manifest.csv`：登记每个数据文件的来源、大小、用途和 Git 策略。
- `checksums.sha256`：记录每个原始数据文件的 SHA256。
- `backup_notes.md`：记录本地或私有备份位置。

大体积原始数据默认不进入普通 Git，但不能只记录来源；必须至少保存一份本地或私有备份。
