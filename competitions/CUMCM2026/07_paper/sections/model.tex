\section{模型建立}

\subsection{决策变量与参数}
\placeholder{定义决策变量、状态变量、参数和约束集合。}

\subsection{目标函数}
在明确优化目标后，可将模型写为
\begin{equation}
  \min_{\bm{x}\in\Omega} F(\bm{x})
  = \placeholder{目标函数表达式}.
\end{equation}
\placeholder{用真实建模推导替换上式中的占位表达式。}

\subsection{约束条件}
模型约束可统一表示为
\begin{equation}
  g_i(\bm{x}) \leq 0,\quad i=1,2,\ldots,m.
\end{equation}
\placeholder{根据赛题条件写出具体约束，不得引入未经说明的限制。}
